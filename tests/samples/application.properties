# Intentionally misconfigured Spring Boot application.properties
# Used for scanner verification only.

# SPRING-001: All actuator endpoints exposed
management.endpoints.web.exposure.include=*

# SPRING-002: Debug mode enabled
debug=true

# SPRING-003: H2 console enabled
spring.h2.console.enabled=true

# SPRING-006: Plaintext database password
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=supersecretdbpass

# SPRING-007: SSL disabled
server.ssl.enabled=false

# SPRING-008: Request details logging
spring.mvc.log-request-details=true
