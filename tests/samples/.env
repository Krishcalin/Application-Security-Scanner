# Intentionally vulnerable .env — scanner test target only
# DO NOT use these values in a real application

NODE_ENV=development

# Weak JWT / session secrets
JWT_SECRET=secret
SESSION_SECRET=changeme

# Wildcard debug logging
DEBUG=*

# MongoDB URI with embedded credentials
MONGODB_URI=mongodb://admin:password123@localhost:27017/myapp

# Service credentials
DB_PASSWORD=P@ssw0rd!
REDIS_PASSWORD=redis_secret_123
SMTP_PASSWORD=smtp_pass_456
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
STRIPE_SECRET_KEY=EXAMPLE_STRIPE_KEY_DO_NOT_USE_IN_PRODUCTION

# CORS wildcard
CORS_ORIGIN=*
ALLOWED_ORIGINS=*

PORT=3000
